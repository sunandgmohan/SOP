SONARQUBE


installation
docker run -d --name sonar -p 9000:9000 --restart sonarqube:lts-community

2. ip:9000
3.admin admin
4.configuration- webhook- create- sonar
                                  jenkinsurl/sonarqube-webhook   eg-http://172.17.0.1:8080/sonarqube-webhook
                                  
5.create
6.security- users- update token- generate 
7. jenkins - credentials - add - secret text- save token there
8.jenkins- systems - sonarqubeservers- givename- add the token here  

environment {
        HARBOR_URL = "172.18.0.1"
        HARBOR_CREDS = "harbor-creds"
        PROJECT_NAME = "crm-backend"
        SONAR_HOME= tool 'sonar'
        IMAGE_NAME = 'gateway'
        IMAGE_TAG =  'latest'
        }
stage ('SonarQube quality analysis'){
          steps {
                withSonarQubeEnv("sonar") {
                    sh "$SONAR_HOME/bin/sonar-scanner -Dsonar.projectName=CRM-Backend -Dsonar.projectKey=CRM-Backend"
                      }
                   }
                 }

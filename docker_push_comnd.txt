DOCKER PUSH

1. 	Build image

      docker build -t projectname .
      
2. Docker tag

   docker tag projectname dockerusername/reponame:tag
   
3. Docker push
  
   docker push dockerusername/reponame:tag
